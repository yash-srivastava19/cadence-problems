known-bad: not detected at all. The scoring line is inside the region, so the
known-bad: model can rewrite what it is judged by. check reports the project
known-bad: as fine. Nothing here asserts an error because there is none.
